---

---

<>
  <section id="Quote">
    <!-- <img
      src="src/assets/quote.svg"
      alt=""
      class="absolute h-full z-[-1] left-[40%] translate-x-[-50%] top-0"
    /> -->
    <div class="container">
      <div class="info-container">
        <h2>LET'S CONNECT</h2>
        <p>
          We'd love to hear from you! Whether you have a question, need
          assistance, or just want to schedule or expedite your load.
          <br />
          <br />
          Feel free to reach out. Our team is here to help you deliver your freight
          on same day and satisfy your customers needs.
          <br />
          <br />
          Send us an email sharing the load's details including Zip Code (From -
          To), Weight, Pieces and any Special Care.
        </p>
        <a href="email:info@widelogistics.com"> info@widelogistics.com</a>
      </div>
      <div class="form-container">
        <form action="" class="flex flex-col gap-3">
          <div class="input-container">
            <fieldset class="flex flex-col gap-1 w-[45%]">
              <label for="name" class="text-2xl font-medium text-white"
                >Name</label
              >
              <input
                type="text"
                name="name"
                class="border-[2px] border-border rounded-md p-2 text-lg bg-white"
              />
            </fieldset>
            <fieldset class="flex flex-col gap-1 w-[45%]">
              <label for="emil" class="text-2xl font-medium text-white"
                >Email</label
              >
              <input
                type="email"
                name="emil"
                class="border-[2px] border-border rounded-md p-2 text-lg bg-white"
              />
            </fieldset>
            <fieldset class="flex flex-col gap-1 w-[45%]">
              <label for="phone" class="text-2xl font-medium text-white"
                >Phone</label
              >
              <input
                type="tel"
                name="phone"
                class="border-[2px] border-border rounded-md p-2 text-lg bg-white"
              />
            </fieldset>
            <fieldset class="flex flex-col gap-1 w-[45%]">
              <label for="services" class="text-2xl text-white font-medium"
                >Service</label
              >
              <input
                type="text"
                name="services"
                class="border-[2px] border-border rounded-md p-2 text-lg bg-white"
                placeholder="Expedited"
                disabled
              />
            </fieldset>
          </div>
          <fieldset class="fieldset-textarea">
            <label for="description" class="text-2xl text-white font-medium"
              >How can we Help?</label
            >
            <textarea
              name="description"
              id="description"
              class="border-[2px] border-border rounded-md p-2 text-lg bg-white"
            ></textarea>
          </fieldset>
          <button
            class="py-4 px-9 bg-amber-500 font-bold text-2xl text-white rounded-lg"
          >
            Send
          </button>
        </form>
      </div>
    </div>
  </section>
  <script is:inline>
    const form = document.querySelector("form");
    function sendEmail() {
      Email.send({
        Host: "smtp.elasticemail.com",
        Username: "footpath_urology427@slmail.me",
        Password: "508E03E18A5134309D6A40821CACA4D38164",
        To: "footpath_urology427@slmail.me",
        From: "footpath_urology427@slmail.me",
        Subject: "This is the subject",
        Body: "And this is the body",
      }).then((message) => alert(message));
    }

    form.addEventListener("submit", (e) => {
      e.preventDefault();

      sendEmail();
    });
  </script>
  <style>
    .info-container {
      max-width: 100%;
    }
    .container {
      display: flex;
      flex-direction: column;
      gap: 30px;
    }
    .info-container a {
      color: white;
      font-size: clamp(16px, 2vw, 28px);
      text-decoration: none;
    }
    p {
      color: white;
      font-size: clamp(16px, 2vw, 28px);
    }
    button:hover {
      filter: brightness(110%);
    }
    button {
      font-size: 40px;
      font-size: 24px;
      background-color: #fcb900;
      border: 1px solid #ebad00;
      border-radius: 10px;
      color: white;
      font-weight: 600;
      padding-block: 15px;
      cursor: pointer;
    }
    input:disabled {
      background-color: #ccc;
    }

    input[type="text"],
    input[type="email"],
    input[type="tel"],
    textarea {
      font-size: 16px;
      border: 1px solid #ccc;
      border-radius: 5px;
      padding: 10px;
      font-family: inherit;
    }

    input[type="text"]:focus,
    input[type="email"]:focus,
    input[type="tel"]:focus,
    textarea:focus {
      outline: 3px solid #fcb900;
    }
    label {
      color: white;
      font-weight: 600;
      font-size: 18px;
    }
    fieldset {
      display: flex;
      flex-direction: column;
      gap: 5px;
      border: none;
      padding: 0;
    }
    .input-container {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }
    form {
      display: flex;
      flex-direction: column;
      gap: 20px;
      /* max-width: 900px; */
      width: 100%;
    }
    h2 {
      font-size: 36px;
      font-weight: 800;
      color: white;
      text-align: center;
      margin: 0;
      margin-bottom: 40px;
    }

    .form-container {
      width: 100%;
      display: flex;
      /* place-content: center; */
    }

    section {
      background-color: #1171a7;
      padding-inline: 20px;
      padding-block: 40px;
    }
    @media screen and (min-width: 800px) {
      .input-container {
        flex-direction: row;
        flex-wrap: wrap;
      }
      fieldset {
        width: calc(50% - 20px);
      }
      .fieldset-textarea {
        width: 100%;
      }
      section {
        padding-inline: 60px;
      }
      h2 {
        font-size: 48px;
      }
    }
    @media screen and (min-width: 1000px) {
      .info-container {
        max-width: 50%;
      }
      h2 {
        font-size: 56px;
      }
      .container {
        flex-direction: row;
        justify-content: center;
        align-items: center;
        gap: 30px;
      }
      fieldset {
        width: calc(50% - 15px);
      }
    }
    @media screen and (min-width: 1800px) {
      h2 {
        font-size: 64px;
      }
    }
  </style>
</>
